# Tasks

- [x] 1. Add guided-generation DTOs and Apple Foundation Models planner behind compile-time and runtime availability guards
- [x] 2. Map generated DTOs strictly into registered effect graphs and retain existing validation as the execution boundary
- [x] 3. Add preferred-planner fallback orchestration with explicit provenance and availability/failure reasons
- [x] 4. Wire planner identity and availability into the view model and plan inspector without invoking video rendering
- [x] 5. Add injected-planner tests for semantic prompt mapping, unknown effects, invalid parameters, unavailable model, and generation failure
- [ ] 6. Run focused planner tests, full `swift test`, `swift build`, and strict OpenSpec validation

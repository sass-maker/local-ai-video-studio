# Tasks

- [x] 1. Define the provisional discovery-record JSONL schema and shard manifest
- [x] 2. Author the first 20 prompts across representative, adversarial, ambiguous, and unsupported cases
- [x] 3. Generate the first Devin GLM-5.2 shard with exact run provenance and retain raw responses
- [x] 4. Validate every candidate against the current registry and graph validator without discarding failures
- [x] 5. Produce a human review sheet comparing schema fitness, semantic fidelity, unsupported behavior, and proposed revisions
- [x] 6. Define the minimal semantic-plan schema with effect-specific parameter constraints
- [x] 7. Implement the trusted Swift semantic-plan compiler into strict `EffectGraph` values
- [x] 8. Generate and retain the final 20 semantic plans with exact Devin run provenance
- [x] 9. Validate decisions, variant counts, effects, parameters, compiled graphs, and forbidden boilerplate
- [x] 10. Finalize the human review and schema-freeze criteria; stop without generating a second shard

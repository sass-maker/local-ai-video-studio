# Product

Local AI Video Studio helps a creator turn one source clip into a small set of
understandable, reproducible edit variants without uploading media or learning
a professional editing suite.

The core loop is import → describe → inspect plans → render → compare → export.
Privacy, legibility, and honest render-state feedback are product features.

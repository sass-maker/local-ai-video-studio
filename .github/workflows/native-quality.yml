name: Native Quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: local-ai-video-studio-native-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false
      - uses: astral-sh/setup-uv@v8.1.0
      - name: Install native analyzers
        run: brew install periphery swift-format
      - name: Run native and site quality gates
        run: node scripts/check-code-health.mjs all
